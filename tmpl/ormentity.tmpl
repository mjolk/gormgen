{{define "entity"}}// DON'T EDIT *** generated by ormgen *** DON'T EDIT
package {{.PackageName}}

import (
	"time"
	"github.com/satori/go.uuid"
)

{{range .Tokens}}
type {{.Name}} struct { {{range .Fields}}
{{.Name}} {{.Type}}`json:"{{.JSON}}"`{{end}}
}
{{end}}
{{end}}
